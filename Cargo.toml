[package]
name = "rho-ui"
version = "0.11.0"
edition = "2021"

[dependencies]
makepad-widgets = { path = "../makepad/widgets" }
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true